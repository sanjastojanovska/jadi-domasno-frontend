import React from 'react';

interface Props {}

const CalendarSliderChef: React.FC<Props> = (props) => {
    return (
        <div className="calendar-slider"></div>
    );
};

export default CalendarSliderChef;